name: Build DG 3.7.2
on:
  push:
    branches: ['release/v3.7.1-maintenance-month-reminder']
    paths: ['build3/**', '.github/workflows/build-v3.yml']
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Compile and verify DG 3.7.2
        run: |
          python -m pip install beautifulsoup4
          rm -rf build3/release
          mkdir -p build3/release
          python build3/build_frontend_371.py
          node --check build3/release/app-3.5.js
          node --check build3/release/sw.js
          python build3/verify_build_371.py
      - name: Commit verified 3.7.2 frontend on release branch
        run: |
          git config user.name 'DG Build'
          git config user.email 'build@users.noreply.github.com'
          cp build3/release/index.html build3/release/app-3.5.js build3/release/app-3.5.css build3/release/sw.js build3/release/manifest.json .
          git add index.html app-3.5.js app-3.5.css sw.js manifest.json
          git commit -m 'v3.7.2: geprüften Wartungsmonatsfix bereitstellen' || true
          git push origin HEAD:release/v3.7.1-maintenance-month-reminder
